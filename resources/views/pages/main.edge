<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title> Kleinanzeigen </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"
    defer></script>
  {{--@vite(['resources/css/app.css', 'resources/js/app.js'])--}}
</head>

<body style="background: lightblue">
  <nav class="bg-light navbar navbar-expand-lg ms-4 me-4 mb-4 p-3 sticky-top border rounded-bottom shadow-lg">

    <div class="collapse navbar-collapse d-flex flex-column flex-lg-row justify-content-between container-fluid">
      <a class="navbar-brand fw-bolder" href="{{ route('home')}}">Sell'n'Sells</a>

      <div class="d-flex justify-content-around">
        @if(auth.isAuthenticated)
        <a class="btn btn-primary mx-2" href="{{ route('addProduct')}}">+</a>
        {{--<a class="btn btn-secondary mx-2" href="{{ route('chats')}}">Chats</a>--}}
        <a class="btn btn-primary mx-2" href="{{ route('profile')}}">Profile</a>
        <a class="btn btn-danger mx-2" href="{{ route('logout')}}">Logout</a>
        @else
        <a class="btn btn-primary mx-2" href="{{ route('register')}}">Register</a>
        <a class="btn btn-primary mx-2" href="{{ route('login')}}">Login</a>
        @endif
      </div>
    </div>
    </div>
  </nav>

  @include(template)

  <footer class="bg-secondary text-white me-4 ms-4 rounded-top p-3">
    <div class="container text-center">
      <p>&copy; 2024 SellnSells.com</p>
    </div>
  </footer>

</body>

</html>